import React from "react";
import { View } from "react-native";

import { withTheme } from "@beppla/tapas-ui";

const PurchaseCostContainer: React.FC = ({ theme }: { theme: any }) => {
  return <View></View>;
};

export default withTheme(PurchaseCostContainer);
